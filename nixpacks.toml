[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --prefix backend",
  "echo 'Backend dependencies installed'"
]

[phases.build]
cmds = ["echo 'No build step required - ready to start'"]

[start]
cmd = "node server.js"
